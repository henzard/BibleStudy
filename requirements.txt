# BibleStudy Project Dependencies
# Install with: pip install -r requirements.txt

# Core dependencies
feedparser>=6.0.0        # RSS/ATOM feed parsing
requests>=2.31.0         # HTTP requests

# Environment variables
python-dotenv>=1.0.0     # Load .env files

# Optional: AI enhancement (newsletters + early-warning pipeline)
# The early-warning pipeline is provider-agnostic. Install whichever SDK you
# plan to use; with neither installed it runs on a deterministic offline
# heuristic backend (no network, no keys required).
openai>=1.0.0           # OpenAI backend (and newsletter polish)
# anthropic>=0.39.0     # Anthropic (Claude) backend — uncomment to enable

# Optional: Machine learning / analytics
numpy>=1.24.0           # Numerical computing
tensorflow>=2.15.0      # ML predictions (optional, large install)

# Note: TensorFlow is optional and large (~500MB)
# If you don't need ML predictions, you can skip installing tensorflow
# Basic functionality works without it

# Development / testing
pytest>=7.0.0           # Test suite (tests/)
